export interface IPostType {
    author: string;
    blurb: string;
    body: string;
    created_at: string; // not right
    id: number;
    img_url: string;
    title: string;
    updated_at: string; // not right
}
